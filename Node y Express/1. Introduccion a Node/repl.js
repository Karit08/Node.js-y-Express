/*
* By: Karla Itzel Jimenez Molina 
*/

function saludar(nombre) {
    return `Hola, ${nombre}`;
};
  
saludar("Kar");

// Para ejecutar el archivo se escribe (node + nombre del archivo = node repl.js )
//esto es porque node es el entorno de ejecucion para de Js sin necesidad de tener la presencia de un navegador.
